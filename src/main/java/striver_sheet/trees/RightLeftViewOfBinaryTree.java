package striver_sheet.trees;

public class RightLeftViewOfBinaryTree {

    // Left view of Binary tree...
    // To capture all the nodes visible when viewed the Binary tree from Right - some way we need level
    // Reversed Preorder - Curr, Right, Left
    // capture the node only once at each level use a ds and level pass level at each recursion call and capture
    // when ds.size() == level
    public static void main(String[] args) {


    }
}
