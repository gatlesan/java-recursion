package striver_sheet.trees;

public class VerticalOrderTraversal {

    // traversing nodes vertically from left to right
    // If nodes are overlapping add them in asc order

    // Intuition - draw an x-axis when gives vertical nodes -2,-1,0,1,2 and y-axis which gives levels
    public static void main(String[] args) {

    }
}
