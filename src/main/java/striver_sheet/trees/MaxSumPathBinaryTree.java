package striver_sheet.trees;

public class MaxSumPathBinaryTree {

    public static void main(String[] args) {

        // *** similar pattern - max height of the tree...
        // *** Look if nodes contains negative values - check max(0, maxPath(node.left)), max(0, maxPath(node.right))
        // ***  val + (leftMaxPath + rightMaxPath)

    }
}
