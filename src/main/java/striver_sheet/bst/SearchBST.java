package striver_sheet.bst;

public class SearchBST {

    // *** search in BST is always less than search in BT O(n) > O((log)(n)) base 2 log(n)
    // *** Always be height of the BST
    // search using iterative approach.... while loop
    // while(root != null && root != val)
    // root = root.data > val ? root.left : root.right
    // return root (null or node with val)

}
